#include <iostream>
#include <queue>
#include <stack>
using namespace std;
int main()
{
    // queue<int> q;
    // q.push(1);
    // q.push(2);
    // q.push(3);
    // q.push(4);
    // stack<int> st;
    // while (q.empty() != true)
    // {
    //     cout << q.front() << endl;
    //     st.push(q.front());
    //     q.pop();
    // }
    // while (st.empty() != true)
    // {
    //     q.push(st.top());
    //     st.pop();
    // }
    // cout << "REVERSE OF QUEUE" << endl;
    // while (q.empty() != true)
    // {
    //     cout << q.front() << endl;
    //     q.pop();
    // }
    
}